{# Stage 1 Step 3: 详细卡纲生成 User Prompt #}

-----

**核心定义**:
1. **故事单元 (Story Unit)**: 一卡 10 集拆分为 **2 个故事单元**，每个单元包含若干连续集数，形成一个相对完整的剧情闭环。
   - 常见结构：`23+23`、`23+32`、`32+23`、`33+31`、`33+4`、`4+33`、`32+32`、`3+331`等（前后数字相加均为 10）
   - 如 `23+23` 表示：单元 1 为 5 集 (2+3)，单元 2 为 5 集 (2+3)
   - 如 `23+32` 表示：单元 1 为 5 集 (2+3)，单元 2 为 5 集 (3+2)
   - 如 `32+23` 表示：单元 1 为 5 集 (3+2)，单元 2 为 5 集 (2+3)
   - 如 `33+31` 表示：单元 1 为 6 集 (3+3)，单元 2 为 4 集 (3+1)
   - 如 `33+4` 表示：单元 1 为 6 集 (3+3)，单元 2 为 4 集 (4)
   - 如 `4+33` 表示：单元 1 为 4 集 (4)，单元 2 为 6 集 (3+3)
   - 如 `32+32` 表示：单元 1 为 5 集 (3+2)，单元 2 为 5 集 (3+2)
   - 如 `3+331` 表示：单元 1 为 3 集 (3)，单元 2 为 7 集 (3+3+1)，一般而言，这种结构用在第一卡比较多，最后1集基本都是大爽点+大钩子。

2. **故事节点 (Story Beat)**: 每个故事单元内的数字代表一个节点，节点是最小的剧情推进单位。
   - 如 `23+23` 共 4 个节点：2、3、2、3
   - 如 `23+32` 共 4 个节点：2、3、3、2
   - 如 `32+23` 共 4 个节点：3、2、2、3
   - 如 `33+4` 共 3 个节点：3、3、4
   - 如 `4+33` 共 3 个节点：4、3、3
   - 其他不再一一列举，自行采用、推理
   - 特殊：若最后一位为 1（如 `33+31`），该 1 集作为**全卡钩子/高潮**，不算独立节点

3. **爽点分布**: 
   - 每个节点至少有 **1 个小爽点**
   - 每个单元收束时有 **1 个较大爽点**

4. **衔接处理**: 每卡不是互相独立的剧情，各卡之间上下串联，是一个逻辑连贯的整体，不要写“离散”的剧情，要写整体连贯的剧情。

-----

【重要：当前任务】

**输出指令**:
你正在生成短剧的详细卡纲，需要为指定的卡片生成故事单元结构。

**当前需要生成的卡片**: 卡 {{ card_ids | join(', ') }}
{% if card_ranges %}
**各卡负责的全剧集数范围**（episodes 必须严格按此划分，不可交错或遗漏）:
{% for cid, rng in card_ranges.items() %}- 卡 {{ cid }}: 第 {{ rng }} 集
{% endfor %}
{% endif %}

**输出要求**:
1. 请生成 JSON 对象，包含 `detailed_cards` 列表。
2. 每个卡片对象包含:
   - `card_id`: 卡片编号 (1-{{ card_count }})
   - `structure`: 故事单元分隔模式，如 "23+32", "23+23", "33+4" 等
   - `story_units`: 故事单元数组，每个单元包含:
     - `unit_id`: 单元编号 (1, 2, ...)
     - `episodes`: 集数范围，如 "1-5", "6-10"
     - `pattern`: 单元内章节分布，如 "2+3", "3+2", "4"
     - `summary`: 该故事单元的完整梗概 (200字以内，需分节点来写内容！)

3. 每一卡{{ episodes_per_card }}集，必须拆分为至少2个故事单元（集数必须落在该卡负责的全剧集数范围内，且各卡集数范围连续、不重叠）
4. 结构模式说明:
   - "23+32" = (2+3) + (3+2)，两个单元各5集
   - "33+4" = (3+3) + (4)，两个单元分别6集和4集
   - 组合不限只要集数总和为{{ episodes_per_card }}，且能自圆其说即可

**JSON 示例**:
```json
{
  "detailed_cards": [
    {
      "card_id": 1,
      "structure": "23+32",
      "story_units": [
        {"unit_id": 1, "episodes": "1-5", "pattern": "2+3", "summary": "主角重生第一天，发现金手指能力...（200字以内，每个故事节点必须独立标注！）"},
        {"unit_id": 2, "episodes": "6-10", "pattern": "3+2", "summary": "主角利用能力初次打脸Boss1...（200字以内，每个故事节点必须独立标注！）"}
      ]
    }
  ]
}
```

**注意事项**:
请遵照格式，仅返回 JSON。
注意区分"单元"与"节点"，不可混为一谈。

-----

**用户设计**:
{{ concept }}
{% if detail_instruction %}
**重要-用户当前阶段详细指令**：{{ detail_instruction }}
{% endif %}

- 注意：用户输入为最高优先级，不可违背。

**Step 1 产出: 梗概与人设**
{{ synopsis }}

**Step 2 产出: 完整{{ card_count }}卡粗大纲**
{{ rough_skeleton }}

{% if existing_detailed_cards %}
**已有的详细卡纲 (请保持一致性)**:
{{ existing_detailed_cards }}
{% endif %}

**重要：任务目标**
目标1：基于粗大纲，为指定卡片(卡 {{ card_ids | join(', ') }})选择合理的故事单元结构，确保剧情衔接顺畅。
目标2：每个故事单元都要有明确的节点标注，剧情梗概要详细具体。
目标3：按照要求的 JSON 格式输出。

