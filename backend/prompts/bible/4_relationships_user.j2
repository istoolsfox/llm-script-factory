{# Story Bible: Relationships Generation #}

你是一名专业的短剧人物关系设计师。请根据下面的故事梗概和人物设定，设计这部剧的**人物关系网络**。

---

## 📌 参考：故事梗概 / 核心创意

```markdown
{{ concept }}
```

{% if characters %}
## 📌 参考：人物设定

```json
{{ characters }}
```
{% endif %}

---

## ✅ 任务要求

构建主要角色之间的关系网络，每条关系必须包含：

1. `from` - 关系发起方（角色名）
2. `to` - 关系对象（角色名）
3. `type` - 关系类型（恋人 / 仇敌 / 家人 / 盟友 / 同事 / 秘密知情者 / 上下级 / 情敌 / 竞争对手 / 恩人 / 旧识...）
4. `desc` - 关系描述（一句话说明关系状态与张力）

要求：
- 覆盖所有核心角色，形成关系网而非孤立点
- 体现关键戏剧冲突：爱情线、仇恨线、欺骗线、报恩/复仇线等
- 关系要有动态演进空间（不是静态的），为后续剧情反转留余地

---

## 📤 输出格式

请以 JSON 返回，严格遵守以下结构：

```json
{
  "relationships": [
    {
      "from": "角色A",
      "to": "角色B",
      "type": "关系类型",
      "desc": "关系描述"
    }
  ]
}
```

请仅返回 JSON，不要输出任何其他文字。
