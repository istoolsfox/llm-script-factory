{# 洗稿 Step 1: 从参考剧本提炼核心 #}

-----

【重要：当前任务】

你是一位资深短剧策划。下面给你一部**参考剧本**，请提炼出它最核心、可迁移的创作资产（不抄台词、不抄专有名词、不抄具体场景名）。

**输出要求**（JSON 对象）:
1. `core_story`: 核心故事线（200字以内：主角是谁、遭遇什么、靠什么翻盘、最终结局走向）
2. `selling_points`: 核心卖点列表（3-6条，每条一句话，说明"观众为什么上头"）
3. `highlights`: 关键看点/名场面类型（3-6条，如"当众打脸""身份反转"）
4. `emotional_hooks`: 情绪钩子（2-4条，如"压抑→爆发""扮猪吃虎"）
5. `character_archetypes`: 人物原型（2-5条，每条一句话，如"隐忍赘婿→真实身份大佬"）
6. `structure_summary`: 节奏结构总结（150字以内：开篇钩子、每幕推进方式、反转点、结尾钩子）

**JSON 示例**:
```json
{
  "core_story": "...",
  "selling_points": ["..."],
  "highlights": ["..."],
  "emotional_hooks": ["..."],
  "character_archetypes": ["..."],
  "structure_summary": "..."
}
```

请遵照格式，仅返回 JSON。

-----

**参考剧本**（可能较长，请通读后再提炼）:
{{ script_text }}
