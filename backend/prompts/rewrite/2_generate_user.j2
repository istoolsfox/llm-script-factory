{# 洗稿 Step 2: 依据核心提炼生成换皮新故事 #}

-----

【重要：当前任务】

你是一位资深短剧策划。下面是同一赛道的成功剧本的**核心提炼**（故事骨架、卖点、看点、人物原型、节奏结构）。请在这个骨架之上，**换皮**创作一个全新的短剧故事：保留让人上头的结构和爽点逻辑，替换全部题材、背景、行业、人物姓名与具体设定。

{% if instruction %}
**用户的换皮要求**（最高优先级，必须遵守）:
{{ instruction }}
{% endif %}

**输出要求**（JSON 对象）:
1. `title`: 新剧名（吸引眼球，中文，8字以内）
2. `logline`: 一句话故事（50字以内）
3. `concept`: 完整故事概念（600-900字，包含：世界观/背景、主角人设与开局困境、金手指或翻盘逻辑、核心冲突与对手、关键爽点场面设计3个以上、感情线（如有）、大结局走向与续集钩子）。要求与参考剧本卖点一一对应但题材完全不同。
4. `selling_points_mapping`: 卖点对应表（数组，每条对象 {"original": "参考剧本卖点", "new": "新故事如何实现该卖点"}）

**JSON 示例**:
```json
{
  "title": "...",
  "logline": "...",
  "concept": "...",
  "selling_points_mapping": [{"original": "...", "new": "..."}]
}
```

请遵照格式，仅返回 JSON。

-----

**参考剧本核心提炼**:
{{ analysis }}
